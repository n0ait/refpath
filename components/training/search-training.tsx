import { Input } from "@/components/ui/input";

export function Search() {
  return (
      <Input
        type="search"
        placeholder="Recherche un entraînement..."
        className="h-10"
      />
  )
}